# 已知问题与局限性说明

本文档如实说明当前系统在演示边界、工程边界与算法边界上的已知限制，供评审时对照验证。

## 1. 人工审核（Human Review）为单审核员演示模式

- 当前部署仅配置**单一审核员**角色，未实现多审核员并行、审核任务分派与仲裁。
- 审核密码校验接受**任意三位数字**作为演示密码（见 `.env.example` 中 `REVIEW_*` 配置组），仅用于本地演示，**不构成任何安全边界**。生产部署必须替换为正式身份认证体系。
- 审核通过后进入发布门禁（Hard Gate），门禁语义本身不可绕过；但"谁在审核"这一信任前提在演示环境下是弱化的。

## 2. 回放（Replay）模式的外部调用是录制缓存

- 回放模式下，搜索引擎、网页抓取等外部调用从本地录制的缓存（按 `request_fingerprint` 匹配）读取，**不发起真实网络请求**。
- 工作流状态机、证据关联、引用检查、验证引擎在回放中均为**真实执行**——即回放验证的是编排与治理逻辑，而非外部数据的新鲜度。
- 录制数据集对应内置案例 East Palestine 2023；其他主题必须走真实调研链路。

## 3. 真实调研依赖外部服务的可用性

- 真实调研使用 DuckDuckGo Search（`ddgs`）聚合搜索，后端限定为 `bing, duckduckgo, yahoo, brave, wikipedia`，单次调用有 25 秒兜底超时。搜索引擎在部分网络环境下可能超时或返回空结果，此时反馈循环会标记 `BUDGET_EXHAUSTED` 或产生研究缺口，而非静默失败。
- 网页抓取依赖目标站点的可达性与反爬策略，部分站点会返回截断内容或拒绝访问，对应证据会因完整性校验不过关而被丢弃（这是预期行为）。

## 4. 单运行锁：调研串行执行

- 服务端使用 `_run_lock` 保证同一时刻**仅一个调研运行**，后续请求会排队等待。这是为了规避 SQLite 写并发与外部调用配额问题，代价是多用户并发调研时延迟叠加。多实例部署需要引入任务队列与独立的数据库进程。

## 5. SQLite 本地部署边界

- 持久化使用单文件 SQLite（`data/blackboard.db`），黑板表（`mp_*`）与调查表（`inv_*`）共库，由 42 个 append-only 触发器保护调查记录的不可变性。
- SQLite 不适合高并发写入与水平扩展；生产环境应替换为 PostgreSQL/MySQL 并保留触发器语义（Docker Compose 中已含 postgres 服务定义，但应用侧连接串默认仍指向 SQLite）。

## 6. 大模型输出的不确定性已被工程约束，但不能完全消除

- 四个 Agent（Planner/Researcher/Analyst/Verifier）通过 Pydantic 强类型契约（`frozen=True`、`extra="forbid"`）通信，模型输出经过结构化校验与业务校验，失败会触发带修复提示的重试。
- 尽管如此，大模型生成内容本质上存在不确定性：同一输入在不同运行中可能产生不同的声明措辞或任务拆分。系统通过语义哈希（排除运行时元数据）保证**相同语义输入产生稳定哈希**，但声明内容本身仍依赖模型质量。
- 未验证（UNESTABLISHED）与存在争议（DISPUTED）的声明会如实呈现在报告中，不会被模型"圆场"成已验证——这是验证引擎与发布门禁共同保证的硬语义。

## 7. 报告语言的混合现状

- 前端界面与文档已全面中文化；但内置案例（East Palestine）的声明、证据摘录与生成的报告正文为英文（源自英文原始信源）。真实调研中文话题时，声明与报告内容跟随信源语言。报告引擎本身不做翻译。

## 8. 验证引擎的能力边界

- 语义蕴含验证（entailment）基于模型判断 + 证据独立性检查 + 15 阶段策略按声明类型分 profile 执行；对**高度专业领域**（如医学、法律术语密集文本）的判断质量依赖底层模型能力，未做领域微调。
- 冲突检测（Conflict Analysis）目前识别数量级与事实性冲突并如实标注 RESOLVED/UNRESOLVED，不做自动裁决——裁决权保留给人。

## 9. 其他已知小问题

- 前端导出 PDF 使用 jsPDF + html2canvas 截图式渲染，超长报告导出耗时较长；图片型 PDF 不可选中文本（中文渲染依赖系统字体）。
- 调研进行中刷新页面会从本地缓存恢复轮询，但若浏览器清空存储则丢失运行视图（服务端运行仍在继续，可通过调查详情重新打开）。
- 报告章节中 "Probable Findings / Disputed Findings / Quantitative Findings" 等在无对应声明时显示占位文案 `_Insufficient Supported Material_`，这是确定性 Writer 的预期行为而非缺陷。

## 10. 不在当前范围内

以下能力明确**不在**本次交付范围内：

- 多租户与权限体系（RBAC）
- 审核工作流的多级审批（当前为单级）
- 报告多语言翻译
- 分布式部署与水平扩容